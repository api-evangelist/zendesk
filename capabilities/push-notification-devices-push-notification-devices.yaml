naftiko: 1.0.0-alpha2
info:
  label: Zendesk Push Notification Devices — Push Notification Devices
  description: 'Zendesk Push Notification Devices — Push Notification Devices. 1 operations. Lead operation: Zendesk Post  Api
    V2 Push_notification_devices Destroy_many. Self-contained Naftiko capability covering one Zendesk business surface.'
  tags:
  - Zendesk
  - Push Notification Devices
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ZENDESK_API_KEY: ZENDESK_API_KEY
capability:
  consumes:
  - type: http
    namespace: push-notification-devices-push-notification-devices
    baseUri: ''
    description: Zendesk Push Notification Devices — Push Notification Devices business capability. Self-contained, no shared
      references.
    resources:
    - name: api-v2-push_notification_devices-destroy_many
      path: /api/v2/push_notification_devices/destroy_many
      operations:
      - name: pushnotificationdevices
        method: POST
        description: Zendesk Post  Api V2 Push_notification_devices Destroy_many
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
  exposes:
  - type: rest
    namespace: push-notification-devices-push-notification-devices-rest
    port: 8080
    description: REST adapter for Zendesk Push Notification Devices — Push Notification Devices. One Spectral-compliant resource
      per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/api/v2/push-notification-devices/destroy-many
      name: api-v2-push-notification-devices-destroy-many
      description: REST surface for api-v2-push_notification_devices-destroy_many.
      operations:
      - method: POST
        name: pushnotificationdevices
        description: Zendesk Post  Api V2 Push_notification_devices Destroy_many
        call: push-notification-devices-push-notification-devices.pushnotificationdevices
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: push-notification-devices-push-notification-devices-mcp
    port: 9090
    transport: http
    description: MCP adapter for Zendesk Push Notification Devices — Push Notification Devices. One tool per consumed operation,
      routed inline through this capability's consumes block.
    tools:
    - name: zendesk-post-api-v2-push
      description: Zendesk Post  Api V2 Push_notification_devices Destroy_many
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: push-notification-devices-push-notification-devices.pushnotificationdevices
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
