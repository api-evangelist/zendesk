naftiko: 1.0.0-alpha2
info:
  label: Zendesk Users — Basics
  description: 'Zendesk Users — Basics. 1 operations. Lead operation: Zendesk Post  Api V2 Channels Voice Agents Agent_id
    Users User_id Display. Self-contained Naftiko capability covering one Zendesk business surface.'
  tags:
  - Zendesk
  - Basics
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ZENDESK_API_KEY: ZENDESK_API_KEY
capability:
  consumes:
  - type: http
    namespace: users-basics
    baseUri: ''
    description: Zendesk Users — Basics business capability. Self-contained, no shared references.
    resources:
    - name: api-v2-channels-voice-agents-agent_id-users-user_id-display
      path: /api/v2/channels/voice/agents/{agent_id}/users/{user_id}/display
      operations:
      - name: openusersprofileinagentbrowser
        method: POST
        description: Zendesk Post  Api V2 Channels Voice Agents Agent_id Users User_id Display
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: users-basics-rest
    port: 8080
    description: REST adapter for Zendesk Users — Basics. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/api/v2/channels/voice/agents/{agent-id}/users/{user-id}/display
      name: api-v2-channels-voice-agents-agent-id-users-user-id-display
      description: REST surface for api-v2-channels-voice-agents-agent_id-users-user_id-display.
      operations:
      - method: POST
        name: openusersprofileinagentbrowser
        description: Zendesk Post  Api V2 Channels Voice Agents Agent_id Users User_id Display
        call: users-basics.openusersprofileinagentbrowser
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: users-basics-mcp
    port: 9090
    transport: http
    description: MCP adapter for Zendesk Users — Basics. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: zendesk-post-api-v2-channels
      description: Zendesk Post  Api V2 Channels Voice Agents Agent_id Users User_id Display
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: users-basics.openusersprofileinagentbrowser
      outputParameters:
      - type: object
        mapping: $.
