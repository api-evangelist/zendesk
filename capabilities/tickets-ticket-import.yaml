naftiko: 1.0.0-alpha2
info:
  label: Zendesk Tickets — Ticket Import
  description: 'Zendesk Tickets — Ticket Import. 2 operations. Lead operation: Zendesk Post  Api V2 Imports Tickets. Self-contained
    Naftiko capability covering one Zendesk business surface.'
  tags:
  - Zendesk
  - Ticket Import
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ZENDESK_API_KEY: ZENDESK_API_KEY
capability:
  consumes:
  - type: http
    namespace: tickets-ticket-import
    baseUri: ''
    description: Zendesk Tickets — Ticket Import business capability. Self-contained, no shared references.
    resources:
    - name: api-v2-imports-tickets
      path: /api/v2/imports/tickets
      operations:
      - name: ticketimport
        method: POST
        description: Zendesk Post  Api V2 Imports Tickets
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    - name: api-v2-imports-tickets-create_many
      path: /api/v2/imports/tickets/create_many
      operations:
      - name: ticketbulkimport
        method: POST
        description: Zendesk Post  Api V2 Imports Tickets Create_many
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
  exposes:
  - type: rest
    namespace: tickets-ticket-import-rest
    port: 8080
    description: REST adapter for Zendesk Tickets — Ticket Import. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/api/v2/imports/tickets
      name: api-v2-imports-tickets
      description: REST surface for api-v2-imports-tickets.
      operations:
      - method: POST
        name: ticketimport
        description: Zendesk Post  Api V2 Imports Tickets
        call: tickets-ticket-import.ticketimport
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/api/v2/imports/tickets/create-many
      name: api-v2-imports-tickets-create-many
      description: REST surface for api-v2-imports-tickets-create_many.
      operations:
      - method: POST
        name: ticketbulkimport
        description: Zendesk Post  Api V2 Imports Tickets Create_many
        call: tickets-ticket-import.ticketbulkimport
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: tickets-ticket-import-mcp
    port: 9090
    transport: http
    description: MCP adapter for Zendesk Tickets — Ticket Import. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: zendesk-post-api-v2-imports
      description: Zendesk Post  Api V2 Imports Tickets
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: tickets-ticket-import.ticketimport
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: zendesk-post-api-v2-imports-2
      description: Zendesk Post  Api V2 Imports Tickets Create_many
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: tickets-ticket-import.ticketbulkimport
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
