naftiko: 1.0.0-alpha2
info:
  label: Zendesk Activities — Activity Stream
  description: 'Zendesk Activities — Activity Stream. 3 operations. Lead operation: Zendesk Get  Api V2 Activities. Self-contained
    Naftiko capability covering one Zendesk business surface.'
  tags:
  - Zendesk
  - Activity Stream
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ZENDESK_API_KEY: ZENDESK_API_KEY
capability:
  consumes:
  - type: http
    namespace: activities-activity-stream
    baseUri: ''
    description: Zendesk Activities — Activity Stream business capability. Self-contained, no shared references.
    resources:
    - name: api-v2-activities
      path: /api/v2/activities
      operations:
      - name: listactivities
        method: GET
        description: Zendesk Get  Api V2 Activities
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    - name: api-v2-activities-count
      path: /api/v2/activities/count
      operations:
      - name: countactivities
        method: GET
        description: Zendesk Get  Api V2 Activities Count
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    - name: api-v2-activities-activity_id
      path: /api/v2/activities/{activity_id}
      operations:
      - name: showactivity
        method: GET
        description: Zendesk Get  Api V2 Activities Activity_id
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: activities-activity-stream-rest
    port: 8080
    description: REST adapter for Zendesk Activities — Activity Stream. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/api/v2/activities
      name: api-v2-activities
      description: REST surface for api-v2-activities.
      operations:
      - method: GET
        name: listactivities
        description: Zendesk Get  Api V2 Activities
        call: activities-activity-stream.listactivities
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/api/v2/activities/count
      name: api-v2-activities-count
      description: REST surface for api-v2-activities-count.
      operations:
      - method: GET
        name: countactivities
        description: Zendesk Get  Api V2 Activities Count
        call: activities-activity-stream.countactivities
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/api/v2/activities/{activity-id}
      name: api-v2-activities-activity-id
      description: REST surface for api-v2-activities-activity_id.
      operations:
      - method: GET
        name: showactivity
        description: Zendesk Get  Api V2 Activities Activity_id
        call: activities-activity-stream.showactivity
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: activities-activity-stream-mcp
    port: 9090
    transport: http
    description: MCP adapter for Zendesk Activities — Activity Stream. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: zendesk-get-api-v2-activities
      description: Zendesk Get  Api V2 Activities
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: activities-activity-stream.listactivities
      outputParameters:
      - type: object
        mapping: $.
    - name: zendesk-get-api-v2-activities-2
      description: Zendesk Get  Api V2 Activities Count
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: activities-activity-stream.countactivities
      outputParameters:
      - type: object
        mapping: $.
    - name: zendesk-get-api-v2-activities-3
      description: Zendesk Get  Api V2 Activities Activity_id
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: activities-activity-stream.showactivity
      outputParameters:
      - type: object
        mapping: $.
