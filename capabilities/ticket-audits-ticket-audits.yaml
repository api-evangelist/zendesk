naftiko: 1.0.0-alpha2
info:
  label: Zendesk Ticket Audits — Ticket Audits
  description: 'Zendesk Ticket Audits — Ticket Audits. 1 operations. Lead operation: Zendesk Get  Api V2 Ticket_audits. Self-contained
    Naftiko capability covering one Zendesk business surface.'
  tags:
  - Zendesk
  - Ticket Audits
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ZENDESK_API_KEY: ZENDESK_API_KEY
capability:
  consumes:
  - type: http
    namespace: ticket-audits-ticket-audits
    baseUri: ''
    description: Zendesk Ticket Audits — Ticket Audits business capability. Self-contained, no shared references.
    resources:
    - name: api-v2-ticket_audits
      path: /api/v2/ticket_audits
      operations:
      - name: listticketaudits
        method: GET
        description: Zendesk Get  Api V2 Ticket_audits
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: page[before]
          in: query
          type: string
          description: 'A [pagination cursor](/documentation/api-basics/pagination/paginating-through-lists-using-cursor-pagination)
            that tells the endpoint which page to start on. It '
        - name: page[after]
          in: query
          type: string
          description: 'A [pagination cursor](/documentation/api-basics/pagination/paginating-through-lists-using-cursor-pagination)
            that tells the endpoint which page to start on. It '
        - name: page[size]
          in: query
          type: integer
          description: Specifies how many records to be returned in the response. You can specify up to 100 records per page.
  exposes:
  - type: rest
    namespace: ticket-audits-ticket-audits-rest
    port: 8080
    description: REST adapter for Zendesk Ticket Audits — Ticket Audits. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/api/v2/ticket-audits
      name: api-v2-ticket-audits
      description: REST surface for api-v2-ticket_audits.
      operations:
      - method: GET
        name: listticketaudits
        description: Zendesk Get  Api V2 Ticket_audits
        call: ticket-audits-ticket-audits.listticketaudits
        with:
          page[before]: rest.page[before]
          page[after]: rest.page[after]
          page[size]: rest.page[size]
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ticket-audits-ticket-audits-mcp
    port: 9090
    transport: http
    description: MCP adapter for Zendesk Ticket Audits — Ticket Audits. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: zendesk-get-api-v2-ticket
      description: Zendesk Get  Api V2 Ticket_audits
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: ticket-audits-ticket-audits.listticketaudits
      with:
        page[before]: tools.page[before]
        page[after]: tools.page[after]
        page[size]: tools.page[size]
      outputParameters:
      - type: object
        mapping: $.
