naftiko: 1.0.0-alpha2
info:
  label: Zendesk Routing — Job Statuses
  description: 'Zendesk Routing — Job Statuses. 1 operations. Lead operation: Zendesk Post  Api V2 Routing Agents Instance_values
    Job. Self-contained Naftiko capability covering one Zendesk business surface.'
  tags:
  - Zendesk
  - Job Statuses
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ZENDESK_API_KEY: ZENDESK_API_KEY
capability:
  consumes:
  - type: http
    namespace: routing-job-statuses
    baseUri: ''
    description: Zendesk Routing — Job Statuses business capability. Self-contained, no shared references.
    resources:
    - name: api-v2-routing-agents-instance_values-job
      path: /api/v2/routing/agents/instance_values/job
      operations:
      - name: bulksetagentattributevaluesjob
        method: POST
        description: Zendesk Post  Api V2 Routing Agents Instance_values Job
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
  exposes:
  - type: rest
    namespace: routing-job-statuses-rest
    port: 8080
    description: REST adapter for Zendesk Routing — Job Statuses. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/api/v2/routing/agents/instance-values/job
      name: api-v2-routing-agents-instance-values-job
      description: REST surface for api-v2-routing-agents-instance_values-job.
      operations:
      - method: POST
        name: bulksetagentattributevaluesjob
        description: Zendesk Post  Api V2 Routing Agents Instance_values Job
        call: routing-job-statuses.bulksetagentattributevaluesjob
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: routing-job-statuses-mcp
    port: 9090
    transport: http
    description: MCP adapter for Zendesk Routing — Job Statuses. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: zendesk-post-api-v2-routing
      description: Zendesk Post  Api V2 Routing Agents Instance_values Job
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: routing-job-statuses.bulksetagentattributevaluesjob
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
